#include<stdio.h>

int main(){
    int n, num, soma=0, i=1, j=1, k1;
    
    printf("Informe a quantidade de sequencias: ");
    scanf("%i", &n);
    
    while(i <= n){
        // hipotenusa = (co^2 + ca^2)//2
    }
    
    return 0;
}